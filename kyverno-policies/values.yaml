# ClusterPolicy: Audit (report) or Enforce (block). ValidatingPolicy maps Enforce → Deny, Audit → Audit.
validationFailureAction: Audit

# When true, policies include kyverno.io/ignore so Helm apply avoids Kyverno webhook timeouts on large installs.
installation:
  ignoreOnInstall: true
